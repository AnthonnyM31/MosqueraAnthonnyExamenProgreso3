<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="MosqueraAnthonnyExamenProgreso3.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:MosqueraAnthonnyExamenProgreso3"
    Shell.FlyoutBehavior="Disabled"
    Title="MosqueraAnthonnyExamenProgreso3">

    <Shell.FlyoutHeader>
        <Label Text="Mi Navegación"
               FontSize="24"
               HorizontalOptions="Center" />
    </Shell.FlyoutHeader>

    <TabBar>
        <ShellContent Title="Buscar"
                      ContentTemplate="{DataTemplate local:Buscar}" />
        <ShellContent Title="Leer BD" 
                ContentTemplate="{DataTemplate local:LeerBD}" />
    </TabBar>

</Shell>
